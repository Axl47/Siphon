SIPHON_DEFAULT_API_URL=http://localhost:9000
SIPHON_HOST=localhost:5173
# Optional: set to true if you want `vite dev` to use HTTPS.
# If enabled, make sure the API CORS origin also uses https://localhost:5173.
# SIPHON_DEV_HTTPS=false
